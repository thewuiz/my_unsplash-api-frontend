import { HttpTokenInterceptor } from './http.token.interceptor';

describe('Http.Token.Interceptor', () => {
  it('should create an instance', () => {
    expect(new Http.Token.Interceptor()).toBeTruthy();
  });
});
